
// This file re-exports the toast functions from the hooks directory
// This helps maintain backward compatibility with existing code
import { useToast, toast } from "@/hooks/use-toast";

export { useToast, toast };
