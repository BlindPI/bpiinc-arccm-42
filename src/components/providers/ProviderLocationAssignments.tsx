import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function ProviderLocationAssignments() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Provider Location Assignments</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Provider location assignment management</p>
      </CardContent>
    </Card>
  );
}