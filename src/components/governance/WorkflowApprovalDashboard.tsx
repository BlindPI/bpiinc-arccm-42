import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function WorkflowApprovalDashboard() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Workflow Approval Dashboard</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Workflow approval management</p>
      </CardContent>
    </Card>
  );
}